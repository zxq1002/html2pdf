---
phase: 03-ux-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [popup.html, popup.css, popup.js]
autonomous: true
requirements: [FEAT-07]
must_haves:
  truths:
    - "Popup 界面包含状态提示区域（Success/Error Toast）"
    - "导出按钮在处理中状态下被禁用"
    - "默认导出模式（阅读模式/全页模式）在重新打开插件时能正确恢复"
  artifacts:
    - path: "popup.html"
      provides: "Status/Toast container"
    - path: "popup.js"
      provides: "State machine logic and persistence deepening"
---

<objective>
更新 Popup 的 UI 结构与核心逻辑，引入状态机管理并深化配置持久化。

1. 在 popup.html 中添加状态反馈（Toast）容器。
2. 在 popup.css 中定义状态反馈的样式。
3. 在 popup.js 中实现简单的状态机（IDLE, PROCESSING, SUCCESS, ERROR）。
4. 确保“提取正文”开关状态的持久化存储完全符合用户预期。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@popup.html
@popup.js
@popup.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 UI 结构与样式</name>
  <files>popup.html, popup.css</files>
  <action>
    - 在 popup.html 的 footer 中添加 `<div id="statusToast" class="status-toast hidden"></div>`。
    - 在 popup.css 中为 `.status-toast` 添加样式：居中浮动、带背景色（成功绿/失败红）、过渡动画。
    - 优化按钮禁用状态的样式，确保视觉清晰。
  </action>
  <verify>
    - 手动检查 popup.html 是否包含新的 div。
    - 检查 popup.css 是否包含对应的样式规则。
  </verify>
  <done>UI 元素和样式就绪</done>
</task>

<task type="auto">
  <name>Task 2: 实现状态机与持久化深化</name>
  <files>popup.js</files>
  <action>
    - 定义 `STATE = { IDLE: 'idle', PROCESSING: 'processing', SUCCESS: 'success', ERROR: 'error' }`。
    - 创建 `setUIState(state, message)` 函数，根据状态控制：
      - `exportBtn.disabled` (PROCESSING 状态下禁用)。
      - `progress.classList` (仅 PROCESSING 显式)。
      - `statusToast` 的显示内容与颜色。
    - 确保 `loadSettings` 和 `saveSettings` 正确处理 `extractContent` 开关。
    - 在 `exportToPDF` 中使用 `setUIState` 替换零散的状态修改代码。
  </action>
  <verify>
    - 在控制台手动调用 `setUIState('processing')` 观察按钮是否禁用。
    - 检查 settings 中是否包含 `extractContent`。
  </verify>
  <done>逻辑层支持状态切换和持久化</done>
</task>

</tasks>

<success_criteria>
- 界面能够根据导出状态动态变化（按钮禁用、显示进度）。
- 成功导出后弹出 Toast 提示（per D-07 requirement）。
</success_criteria>

<output>
完成任务后，更新 .planning/STATE.md 并创建 SUMMARY.md。
</output>
