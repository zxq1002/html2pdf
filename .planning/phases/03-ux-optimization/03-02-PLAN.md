---
phase: 03-ux-optimization
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified: [content.js, popup.js, tests/ux-logic.test.js]
autonomous: true
requirements: [FEAT-07]
must_haves:
  truths:
    - "Popup 能够接收并显示来自 Content Script 的分步进度更新"
    - "单元测试验证了状态转换逻辑的正确性"
  artifacts:
    - path: "tests/ux-logic.test.js"
      provides: "Test suite for state machine and messaging"
---

<objective>
增强 Content Script 与 Popup 之间的消息通信，实现真实进度反馈，并通过 TDD 验证状态转换逻辑。

1. 修改 content.js，在生成 PDF 的关键阶段发送 `PROGRESS_UPDATE` 消息。
2. 修改 popup.js，接收并实时更新进度条。
3. 编写 TDD 测试，模拟消息发送并验证 Popup 状态机行为。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content.js
@popup.js
@.planning/phases/03-ux-optimization/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 编写状态机单元测试</name>
  <files>tests/ux-logic.test.js</files>
  <behavior>
    - 测试从 IDLE 到 PROCESSING 的转换：按钮禁用，进度条显示。
    - 测试从 PROCESSING 到 SUCCESS 的转换：Toast 显示成功消息，按钮启用（延迟或立即）。
    - 测试从 PROCESSING 到 ERROR 的转换：Toast 显示错误消息，按钮启用。
  </behavior>
  <action>
    - 创建 tests/ux-logic.test.js。
    - 使用 Jest 模拟 chrome API。
    - 导入或模拟 popup.js 中的 `setUIState` 逻辑进行测试。
  </action>
  <verify>
    <automated>npm test tests/ux-logic.test.js</automated>
  </verify>
  <done>测试覆盖所有关键状态转换</done>
</task>

<task type="auto">
  <name>Task 2: 实现分步进度反馈消息</name>
  <files>content.js, popup.js</files>
  <action>
    - 在 content.js 的 `generatePDF` 函数中，在开始捕获、开始渲染、完成阶段调用 `chrome.runtime.sendMessage` 发送进度百分比。
    - 在 popup.js 中添加 `chrome.runtime.onMessage` 监听器，识别 `PROGRESS_UPDATE` 动作并调用 `updateProgress`。
    - 针对 `vector` 导出模式（原生打印），在 popup.js 中模拟阶段性进度。
  </action>
  <verify>
    - 在控制台打印收到的进度消息。
    - 观察进度条是否平滑移动。
  </verify>
  <done>用户可看到真实的阶段性进度</done>
</task>

</tasks>

<success_criteria>
- 导出过程中，进度条不再是一次性跳跃，而是分阶段更新。
- 状态机逻辑通过自动化测试验证。
</success_criteria>

<output>
完成任务后，更新 .planning/STATE.md 并创建 SUMMARY.md。
</output>
