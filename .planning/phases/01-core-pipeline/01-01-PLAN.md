---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [package.json, lib/Readability.js, manifest.json]
autonomous: true
requirements: [FEAT-01]

must_haves:
  truths:
    - "项目具备自动化测试基础环境"
    - "Readability.js 已正确引入并可在扩展中使用"
  artifacts:
    - path: "package.json"
      provides: "Node.js 开发环境与测试配置"
    - path: "lib/Readability.js"
      provides: "网页正文提取算法库"
    - path: "manifest.json"
      provides: "扩展配置，声明 Readability.js"
  key_links:
    - from: "manifest.json"
      to: "lib/Readability.js"
      via: "content_scripts"
---

<objective>
初始化开发与测试环境，并引入核心依赖 Readability.js。

Purpose: 为后续的 TDD 开发提供基础，并引入正文识别的关键库。
Output: 配置完整的 package.json，下载并配置好的 Readability.js。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 package.json 并安装开发依赖</name>
  <files>package.json</files>
  <action>
    1. 运行 `npm init -y` 初始化。
    2. 更新 `package.json` 的 `scripts` 增加 `"test": "jest"`。
    3. 安装 `jest` 和 `jest-environment-jsdom` 作为开发依赖。
    4. 创建 `jest.config.js` 配置环境为 `jsdom`。
  </action>
  <verify>
    <automated>npm list jest && ls jest.config.js</automated>
  </verify>
  <done>测试环境配置完成，可以通过 npm test 运行测试。</done>
</task>

<task type="auto">
  <name>Task 2: 引入 Readability.js 依赖</name>
  <files>lib/Readability.js</files>
  <action>
    1. 使用 `web_fetch` 获取 `https://raw.githubusercontent.com/mozilla/readability/master/Readability.js` 的内容。
    2. 将内容保存到 `lib/Readability.js`。
    3. 检查文件内容是否包含 `class Readability`。
  </action>
  <verify>
    <automated>grep -q "class Readability" lib/Readability.js</automated>
  </verify>
  <done>Readability.js 成功下载到 lib 目录。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 manifest.json</name>
  <files>manifest.json</files>
  <action>
    在 `manifest.json` 的 `content_scripts` 中增加 `lib/Readability.js`，确保它在 `content.js` 之前加载。
  </action>
  <verify>
    <automated>grep "lib/Readability.js" manifest.json</automated>
  </verify>
  <done>扩展声明了 Readability.js 依赖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content Script → DOM | Readability.js reads from untrusted page DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | lib/Readability.js | mitigate | 确保从官方源下载，且不修改其核心提取逻辑 |
| T-01-02 | Information Disclosure | DOM Extraction | mitigate | Readability.js 仅提取正文，不应将敏感信息泄露至外部服务 |
</threat_model>

<verification>
运行 `npm test` (即使目前没有测试用例) 确保环境可用。
检查扩展管理界面确保无配置错误。
</verification>

<success_criteria>
- package.json 存在且包含 jest 依赖。
- lib/Readability.js 存在且内容完整。
- manifest.json 已更新。
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-01-SUMMARY.md`
</output>
