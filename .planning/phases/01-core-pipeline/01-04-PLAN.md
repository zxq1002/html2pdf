---
phase: "01"
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: [popup.js, popup.html]
autonomous: false
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-06]

must_haves:
  truths:
    - "用户界面能够触发不同的导出模式"
    - "导出过程中有清晰的状态反馈"
    - "最终生成的 PDF 符合质量预期"
  artifacts:
    - path: "popup.js"
      provides: "更新后的 UI 控制逻辑"
    - path: "popup.html"
      provides: "更新后的交互界面"
---

<objective>
完善用户界面，提供更直观的模式选择和进度反馈，并进行最终功能验证。

Purpose: 提升用户体验，确保核心功能（FEAT-01/02/03/06）在真实场景下可用。
Output: 完善的插件 Popup 界面。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@popup.js
@popup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 Popup 界面与逻辑</name>
  <files>popup.html, popup.js</files>
  <action>
    1. 在 `popup.html` 中明确“阅读模式” (Readability) 和“全页模式”的选项。
    2. 更新 `popup.js`，确保 `getExportConfig` 正确捕获用户选择。
    3. 优化进度条显示逻辑，根据 content script 返回的消息更新进度。
    4. 增加“矢量 PDF (推荐文字可选)”的提示文字。
  </action>
  <verify>
    <automated>grep "阅读模式" popup.html</automated>
  </verify>
  <done>UI 已更新以支持新的导出模式。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 1 的完整核心链路，包括 Readability 提取、PDF 生成优化和 UI 交互。</what-built>
  <how-to-verify>
    1. 加载扩展程序。
    2. 打开一篇复杂的博客文章（如维基百科或新闻网站）。
    3. 点击插件，选择“阅读模式”和“矢量 (文字可选)”。
    4. 验证生成的 PDF 是否仅包含正文，且文字可选中。
    5. 尝试“全页模式”，验证页面布局是否保留完整。
    6. 检查生成速度是否符合预期（NFR-01）。
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- 用户可以顺畅地在 Popup 中选择导出模式并得到 PDF。
- Readability 提取逻辑在主流网站上表现稳定。
- 全页导出无明显截断。
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-04-SUMMARY.md`
</output>
