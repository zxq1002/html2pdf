---
phase: "01"
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [content.js, manifest.json]
autonomous: true
requirements: [FEAT-01, FEAT-03, FEAT-06, NFR-01]

must_haves:
  truths:
    - "content.js 已集成新的提取逻辑"
    - "PDF 生成链路支持文字可选 (FEAT-06)"
    - "生成速度得到优化 (NFR-01)"
  artifacts:
    - path: "content.js"
      provides: "集成 Readability 提取与 html2pdf 生成的完整逻辑"
  key_links:
    - from: "content.js"
      to: "src/extractor.js"
      via: "Function call"
---

<objective>
在 Content Script 中集成新的提取逻辑，并优化 PDF 生成配置。

Purpose: 完成从内容提取到 PDF 生成的核心闭环，确保生成质量与性能。
Output: 更新后的 content.js。
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/extractor.js
@content.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 manifest.json 引入 extractor.js</name>
  <files>manifest.json</files>
  <action>
    在 `content_scripts` 的 `js` 数组中增加 `src/extractor.js`，位于 `lib/Readability.js` 之后，`content.js` 之前。
  </action>
  <verify>
    <automated>grep "src/extractor.js" manifest.json</automated>
  </verify>
  <done>extractor.js 已声明为 content script。</done>
</task>

<task type="auto">
  <name>Task 2: 集成 Readability 提取逻辑到 content.js</name>
  <files>content.js</files>
  <action>
    1. 修改 `handleExportPDF`，在 `readable` 模式下调用 `src/extractor.js` 提供的接口。
    2. 更新 `extractReadableContent` 函数，将结果包装成适合 PDF 生成的 DOM 结构（包含标题、来源链接、正文）。
    3. 确保样式美观（衬线字体、合理的行间距）。
  </action>
  <verify>
    <automated>grep "extract" content.js</automated>
  </verify>
  <done>内容提取链路已切换到 Readability.js。</done>
</task>

<task type="auto">
  <name>Task 3: 优化 PDF 生成配置与性能</name>
  <files>content.js</files>
  <action>
    1. 优化 `html2pdf` 配置，调整 `html2canvas` 的 `scale` (推荐 2) 和 `useCORS: true`。
    2. 针对 FEAT-06，探索 `html2pdf.js` 是否能生成带文本层的 PDF。如果受限，确保生成的图片 PDF 足够清晰，并在 `popup.js` 中保留 `vector` (window.print) 模式作为文字可选的备选方案。
    3. 性能优化 (NFR-01)：减少 `generatePDF` 中的 `setTimeout` 延迟，改用更精确的 `onload` 检查。
    4. 移除生成过程中不必要的 iframe 注入或优化其清理逻辑。
  </action>
  <verify>
    <automated>grep "scale" content.js</automated>
  </verify>
  <done>PDF 生成链路已优化。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | PDF Metadata | mitigate | 确保生成的 PDF 不包含用户隐私数据，仅包含网页内容和 URL |
</threat_model>

<success_criteria>
- content.js 成功调用 extractor 模块。
- 提取出的内容纯净、无广告。
- 生成时间在标准文章下显著减少。
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-03-SUMMARY.md`
</output>
