---
phase: "01"
plan: "02"
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified: [src/extractor.js, tests/extractor.test.js]
autonomous: true
requirements: [FEAT-01, FEAT-02]

must_haves:
  truths:
    - "提取逻辑能够正确识别文章正文"
    - "能够从 HTML 字符串或 DOM 中提取纯净内容"
  artifacts:
    - path: "src/extractor.js"
      provides: "封装好的 Readability 提取逻辑"
    - path: "tests/extractor.test.js"
      provides: "针对提取逻辑的自动化测试"
---

<objective>
使用 TDD 模式开发核心提取模块，封装 Readability.js 提供稳定的正文识别接口。

Purpose: 确保内容提取逻辑的准确性和健壮性，为 FEAT-01 和 FEAT-02 提供底层支持。
Output: 经过测试的 src/extractor.js 模块。
</objective>

<feature>
  <name>正文内容提取模块</name>
  <files>src/extractor.js, tests/extractor.test.js</files>
  <behavior>
    - 输入一个包含复杂 DOM 的 Document 对象。
    - 输出包含 title, content, excerpt, byline 的对象。
    - 干扰元素（导航、广告等）应在输出的 content 中被剔除。
    - 在没有明显正文的页面应能优雅降级（返回 body 内容）。
  </behavior>
  <implementation>
    使用 Readability.js 的核心逻辑。由于 Readability 是作为一个全局类加载到浏览器中的，但在测试环境下我们需要模拟或引入它。
  </implementation>
</feature>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@lib/Readability.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 编写提取模块测试用例</name>
  <files>tests/extractor.test.js</files>
  <behavior>
    - 测试用例 1: 模拟标准文章 HTML，验证是否能提取出正确的 title 和 content。
    - 测试用例 2: 验证导航栏、侧边栏是否被剔除。
    - 测试用例 3: 验证空页面或异常页面的处理。
  </behavior>
  <action>
    创建 `tests/extractor.test.js`，使用 `jsdom` 模拟 DOM 环境。
    注意：在 Node 环境下测试需要正确引入 `lib/Readability.js`。
  </action>
  <verify>
    <automated>npm test tests/extractor.test.js</automated>
  </verify>
  <done>测试用例编写完成并运行失败（红灯）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现提取模块</name>
  <files>src/extractor.js</files>
  <action>
    实现 `src/extractor.js`。该模块应提供一个 `extract` 方法：
    ```javascript
    function extract(doc) {
      // 实例化 Readability 并返回提取结果
    }
    ```
    考虑兼容 CommonJS (用于测试) 和浏览器全局变量 (用于扩展)。
  </action>
  <verify>
    <automated>npm test tests/extractor.test.js</automated>
  </verify>
  <done>实现代码通过所有测试用例（绿灯）。</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | Extraction Logic | mitigate | 对极大规模 DOM 的提取设置超时或限制递归深度 |
</threat_model>

<success_criteria>
- tests/extractor.test.js 包含至少 3 个核心场景测试。
- src/extractor.js 实现了符合预期的提取接口。
- npm test 通过。
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-02-SUMMARY.md`
</output>
